{
  "name": "simplerthings",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "esbuild --bundle --target=es2020 --sourcemap --outfile=main.js main.ts",
    "serve": "esbuild --bundle --target=es2020 --sourcemap --outfile=main.js --servedir=. main.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.17.18",
    "typescript": "^5.0.4"
  }
}
